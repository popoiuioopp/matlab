function result = BisectionMethod(f, n0, n1, desire_error)
    result = (n0 + n1)/2;
    while () * 100 > desire_error
        if f(n0) * f(result) < 0
            n1 = result;
        else
            n0 = result;
        end   
        result = (n0 + n1)/2;
    end
end